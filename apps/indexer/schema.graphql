type DeepLink @entity {
  id: ID!
  owner: Bytes!
  tokenId: BigInt!

  tokenURI: String!

  elementId: String!
  conservationId: String!

  createdAt: BigInt!
  updatedAt: BigInt

  name: String
  symbol: String
}
